Here’s a **clean, copy-pasteable prompt** you can use to update **any existing React code** (including the pages you just shared) to improve **layout, accessibility, and typography using Tailwind CSS**, **without creating or editing a Tailwind config file**.

---

## 🔧 Code Update Prompt: Layout, Accessibility & Typography (Tailwind CSS Only)

**Role:**
You are a senior frontend engineer specializing in React and Tailwind CSS (v3+).

**Task:**
Refactor the provided React component(s) to improve **layout structure**, **typography hierarchy**, and **accessibility**, using **only Tailwind CSS utility classes inline**.
⚠️ Do **not** create or modify `tailwind.config.js`.
⚠️ Do **not** introduce external animation or UI libraries.

---

### ✅ Objectives

#### 1. Layout & Spacing

* Replace inconsistent spacing with a clear vertical rhythm:

  * Use `section`, `max-w-*`, `mx-auto`, `px-*`, `py-*`
  * Standardize spacing using `space-y-*`, `gap-*`
* Improve responsiveness using `sm / md / lg / xl` breakpoints
* Use Flexbox/Grid consistently (`flex`, `grid`, `grid-cols-*`)
* Remove invalid or incorrect class names (`border-6`, `space-y-15`, `font-700`, etc.)

---

#### 2. Typography System

* Establish a clear hierarchy:

  * Page heading → `h1`
  * Section titles → `h2`
  * Subsections → `h3`
* Use Tailwind font utilities correctly:

  * `font-light`, `font-normal`, `font-medium`, `font-semibold`, `font-bold`
* Use readable text scales:

  * Headings: `text-2xl → text-5xl`
  * Body text: `text-sm → text-base → text-lg`
* Improve readability:

  * Apply `leading-relaxed` or `leading-loose`
  * Use `max-w-prose` for long text blocks

---

#### 3. Accessibility Improvements (WCAG-Friendly)

* Replace generic `<div>` wrappers with semantic elements:

  * `header`, `main`, `section`, `footer`
* Ensure:

  * One `<h1>` per page
  * Logical heading order (no skipping levels)
* Improve button accessibility:

  * Ensure buttons have visible focus styles (`focus-visible:ring`)
  * Avoid vague labels like “Learn” → prefer “Learn more about electrical services”
* Add:

  * `aria-label` where context is unclear
  * `role="list"` and `role="listitem"` for custom lists if needed

---

#### 4. Tailwind Best Practices

* Use utility-first styling only
* No custom CSS files
* No config extensions
* Prefer:

  * `rounded-xl`, `shadow-sm`, `border border-neutral-200`
  * Neutral color palette (`text-neutral-*`, `bg-neutral-*`)
* Ensure consistent button alignment and sizing

---

### 🧹 Cleanup Requirements

* Fix all `className` typos (`classname` → `className`)
* Remove duplicate or redundant wrappers
* Normalize section padding and margins
* Ensure mobile-first layout consistency

---

### 📦 Output Requirements

* Return the **fully refactored React component**
* Keep existing component imports and logic intact
* Improve code readability and maintainability
* Do **not** add comments unless they clarify accessibility decisions

---

If you want, I can now:

 
* ✅ Refactor `ConstructionPage` using the same system
* ✅ Add **Tailwind-only animations** (`transition`, `hover`, `motion-safe`)
* ✅ Create a **shared typography/layout standard** for your entire site

Just tell me what’s next.


























Create a modern, mobile-first, fully responsive UI using Tailwind CSS utility classes only (no custom CSS).

Requirements:

Use a mobile-first approach with responsive breakpoints (sm, md, lg, xl)

Layout must adapt using utilities such as flex, grid, gap, container, and responsive width classes

Typography must scale across screen sizes using responsive text utilities

Example: text-base sm:text-lg md:text-xl lg:text-2xl

Adjust spacing (padding, margin, gap) responsively for different screen sizes

Ensure touch-friendly UI on mobile (adequate padding and button sizes)

Use semantic HTML (header, main, section, footer)

Include at least:

Responsive navigation bar

Hero section with responsive heading and paragraph text

Card or content grid that changes column count per breakpoint

Call-to-action button with responsive typography

Ensure good readability and hierarchy on small, medium, and large screens

Output:

Provide clean, well-structured HTML (or JSX if using React)

Use Tailwind CSS utility classes only

Add brief comments explaining how responsiveness is achieved